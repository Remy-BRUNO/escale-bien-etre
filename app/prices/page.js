import styles from "./page.module.css"
import { benefitsData } from "../data/data"

export default function Prices() {
  const { massages } = benefitsData
  return <main className={styles.main}> </main>
}
